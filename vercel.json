{
  "version": 2,
  "builds": [
    {
      "src": "index.ts",
      "use": "@vercel/node",
      "config": { "includeFiles": ["migrations/001_initial.sql"] }
    }
  ],
  "routes": [
    { "src": "/(.*)", "dest": "index.ts" }
  ]
}
